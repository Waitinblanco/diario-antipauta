import { SectionIndex } from "@/components/SectionIndex";

export const metadata = { title: "Espectáculos" };

export default function EspectaculosPage() {
  return <SectionIndex sectionId="espectaculos" />;
}
