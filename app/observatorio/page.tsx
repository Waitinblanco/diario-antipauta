import { SectionIndex } from "@/components/SectionIndex";

export const metadata = { title: "Observatorio" };

export default function ObservatorioPage() {
  return <SectionIndex sectionId="observatorio" />;
}
